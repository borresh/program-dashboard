# Makes Spring autowire test-class constructor parameters without @Autowired, so
# tests follow the same constructor-injection rule as production code and nothing
# in this project ever needs field injection.
spring.test.constructor.autowire.mode=all
